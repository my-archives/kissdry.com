#!/bin/bash
ssh linode 'cd repos/kissdry \
  && git pull \
  && export NODE_ENV=production \
  && export NEW_RELIC_HOME=$(pwd)/config \
  && ./node_modules/.bin/grunt build \
  && forever restart app.js'
